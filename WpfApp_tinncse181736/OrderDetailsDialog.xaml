<Window x:Class="WpfApp_tinncse181736.OrderDetailsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp_tinncse181736"
        mc:Ignorable="d"
        Title="Order Details" Height="550" Width="700"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock x:Name="txtHeader" Grid.Row="0" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Order Information -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Customer:" FontWeight="Bold" Margin="0,0,10,5"/>
            <TextBlock x:Name="txtCustomer" Grid.Row="0" Grid.Column="1" Margin="0,0,0,5"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Employee:" FontWeight="Bold" Margin="0,0,10,5"/>
            <TextBlock x:Name="txtEmployee" Grid.Row="1" Grid.Column="1" Margin="0,0,0,5"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Order Date:" FontWeight="Bold" Margin="0,0,10,5"/>
            <TextBlock x:Name="txtOrderDate" Grid.Row="2" Grid.Column="1" Margin="0,0,0,5"/>
        </Grid>

        <!-- Order Details List -->
        <DataGrid Grid.Row="2" x:Name="dgOrderDetails" AutoGenerateColumns="False" IsReadOnly="True" 
                  GridLinesVisibility="Horizontal" AlternatingRowBackground="#f5f5f5" 
                  RowBackground="White" BorderBrush="#e0e0e0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product" Binding="{Binding Product.ProductName}" Width="*"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat='{}{0:C}'}" Width="120"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Discount" Binding="{Binding Discount, StringFormat='{}{0:P0}'}" Width="80"/>
                <DataGridTextColumn Header="Total" Binding="{Binding Total, StringFormat='{}{0:C}'}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Total and Close Button -->
        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <TextBlock Text="Total Order Amount: " FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtTotalAmount" FontWeight="Bold" VerticalAlignment="Center"/>
            </StackPanel>

            <Button Grid.Column="1" Content="Close" Width="100" Height="30"
                    Click="btnClose_Click" Background="#3498db" Foreground="White"/>
        </Grid>
    </Grid>
</Window>
